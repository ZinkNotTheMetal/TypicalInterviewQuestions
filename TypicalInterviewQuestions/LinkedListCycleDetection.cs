using System.Collections.Generic;

namespace TypicalInterviewQuestions
{
    //#2 in Amazon / Google Questions
    public class LinkedListCycleDetection
    {
        //Linked list contains a cycle if:
        // The malformed list has no end (no node ever has a null next_node pointer)
        // The malformed list contains two links to the same node
        // The malformed list will yield all nodes in the loop multiple times
        public void Validate<T>(LinkedList<T> input)
        {
            
        }
    }
}
